{{ 
try: 
    import misc
    f = form
except:
    exit()
pass
}}
<table>
    {{=TR(misc.html_custom_form('tr_date',form, view=True)) }}
    {{=TR(misc.html_custom_form('requested_by',form, view=True)) }}
    {{=TR(misc.html_custom_form('remarks', form, view=True)) }}
</table>
